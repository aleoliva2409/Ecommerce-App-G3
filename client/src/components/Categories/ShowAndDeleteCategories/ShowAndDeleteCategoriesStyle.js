export const categoriesShow_Delete = ({ breakpoint, spacing }) => ({
  categoriesContainer: {
    display: "flex",
    justifyContent: "center",
  },
  categoriesBox: {
    width: "60%",
    backgroundColor: "#ffffff",
    margin: spacing(2),
    borderRadius: "15px"
  }
})

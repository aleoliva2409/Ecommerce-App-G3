import {makeStyles} from '@material-ui/core/styles';

const useStylesDark = makeStyles(({ breakpoints, spacing, theme }) => ({
    root: {
        backgroundColor: "#01838D",
      }
}))

export { useStylesDark };